{% extends ("layout.html.twig") %}


{% block title %} Modifier le profil {% endblock %}



{% block main %}

<div class="container">

    <div class="row center-align">
        <div class="col s4 offset-s4 m4 offset-m4 l4 offset-l4 form">
            <h3 class="titre">Mon profil</h3>
            {{ form_start(userForm) }}

            {{  form_row(userForm.username) }}
            {{  form_row(userForm.prenom) }}
            {{  form_row(userForm.nom) }}
            {{  form_row(userForm.telephone) }}
            {{  form_row(userForm.email) }}
            {{  form_row(userForm.password) }}
            {{  form_row(userForm.site) }}

        </div>
    </div>

{#   afficher si admin et si actif #}
{#    <div class="row center-align">#}
{#            {% if  user.administrateur == true %}#}
{#                <p>Administrateur</p>#}
{#            {% endif %}#}
{#    </div>#}
{#    <div class="row center-align">#}
{#        {% if  user.actif == true %}#}
{#            <p>Utilisateur actif</p>#}
{#        {% endif %}#}
{#    </div>#}


    <div class="row">
        <div class ="col s4 l3 m3 offset-s3 offset-m4 offset-l4">
                <button type="submit" class="btn waves-effect waves-light">Enregistrer</button>
            {{ form_end(userForm) }}
        </div>
        <div class ="col s4 l3 m3">
            <a href="{{ path('main') }}">
                <input type="button" class="btn btn-secondary" name="annuler" value="Annuler">
            </a>
        </div>
        <br>
        <br>
    </div>
</div>

{%  endblock %}